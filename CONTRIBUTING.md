# Contributing

1. Create an issue that explains the defect or proposed evaluation case.
2. Keep every case deterministic and supported by a read-only expected query.
3. Never add real customer data, API keys, invented testimonials, or unlabeled synthetic results.
4. Run `pytest -q` before opening a pull request.
5. Explain any change to scoring or SQL policy in the pull request description.

